# =============================================================================
# English Learning Platform - Environment Configuration Example
# =============================================================================
# Copy this file to .env and fill in your actual values
# Do not commit the actual .env file to version control
# =============================================================================

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================
# SQLite database file path (relative to project root)
DATABASE_URL="file:./prisma/dev.db"

# =============================================================================
# NEXTAUTH.JS CONFIGURATION
# =============================================================================
# Secret for NextAuth.js session encryption (generate a strong secret)
# Generate with: openssl rand -base64 32
NEXTAUTH_SECRET="+10JNUMJ3iHjN94kQP5rblUV7IiECqnPlekqMzQcWy4="

# URL where the application is running
NEXTAUTH_URL="http://localhost:3000"

# =============================================================================
# GOOGLE OAUTH CONFIGURATION (Optional)
# =============================================================================
# Google OAuth credentials for Google Sign-In
# Get these from: https://console.cloud.google.com/
GOOGLE_CLIENT_ID="your-google-client-id"
GOOGLE_CLIENT_SECRET="your-google-client-secret"

# =============================================================================
# GOOGLE MEET API CONFIGURATION (Optional)
# =============================================================================
# Google Meet API credentials for video lesson integration
# Get these from: https://console.cloud.google.com/
GOOGLE_MEET_CLIENT_ID="your-google-meet-client-id"
GOOGLE_MEET_CLIENT_SECRET="your-google-meet-client-secret"

# =============================================================================
# EMAIL CONFIGURATION - SMTP SETTINGS
# =============================================================================
# SMTP Server Configuration for sending emails
# Example using Hostinger (replace with your email provider)

# SMTP Server Host
SMTP_HOST="smtp.hostinger.com"

# SMTP Server Port
# Common ports: 25 (unencrypted), 587 (TLS), 465 (SSL)
SMTP_PORT="465"

# SMTP Security (true for SSL, false for TLS)
# Use true for port 465 (SSL), false for port 587 (TLS)
SMTP_SECURE="true"

# SMTP Authentication
SMTP_USER="noreply@herliva.com"
SMTP_PASS="Abd@3412"

# Email From Configuration
SMTP_FROM_NAME="English Learning Platform"
SMTP_FROM_EMAIL="noreply@herliva.com"

# =============================================================================
# LEGACY EMAIL CONFIGURATION (For backward compatibility)
# =============================================================================
# These are used by some parts of the application
EMAIL_SERVER_HOST="smtp.hostinger.com"
EMAIL_SERVER_PORT="465"
EMAIL_SERVER_USER="noreply@herliva.com"
EMAIL_SERVER_PASSWORD="Abd@3412"
EMAIL_FROM="noreply@herliva.com"

# =============================================================================
# APPLICATION CONFIGURATION
# =============================================================================
# Application Name
APP_NAME="English Learning Platform"

# Application URL
APP_URL="http://localhost:3000"

# =============================================================================
# NODE ENVIRONMENT
# =============================================================================
# Set to 'development' for local development, 'production' for production
NODE_ENV="development"

# =============================================================================
# EMAIL PROVIDER EXAMPLES
# =============================================================================
# Common email provider configurations:

# HOSTINGER (Current Configuration):
# SMTP_HOST="smtp.hostinger.com"
# SMTP_PORT="465"
# SMTP_SECURE="true"
# SMTP_USER="noreply@herliva.com"
# SMTP_PASS="Abd@3412"

# GMAIL (Example):
# SMTP_HOST="smtp.gmail.com"
# SMTP_PORT="587"
# SMTP_SECURE="false"
# SMTP_USER="your-gmail@gmail.com"
# SMTP_PASS="your-app-password"  # Use app-specific password

# OUTLOOK (Example):
# SMTP_HOST="smtp.office365.com"
# SMTP_PORT="587"
# SMTP_SECURE="false"
# SMTP_USER="your-email@outlook.com"
# SMTP_PASS="your-password"

# YAHOO (Example):
# SMTP_HOST="smtp.mail.yahoo.com"
# SMTP_PORT="465"
# SMTP_SECURE="true"
# SMTP_USER="your-email@yahoo.com"
# SMTP_PASS="your-app-password"

# =============================================================================
# SERVER CONFIGURATION
# =============================================================================
# Port for the development server (default: 3000)
PORT="3000"

# Host for the development server (default: 0.0.0.0)
HOST="0.0.0.0"

# =============================================================================
# SECURITY NOTES
# =============================================================================
# 1. Never commit your actual .env file to version control
# 2. Use strong, randomly generated secrets
# 3. Use app-specific passwords for email services when available
# 4. Regularly rotate your secrets and passwords
# 5. Keep your .env file secure and only share with trusted team members

# =============================================================================
# TESTING YOUR CONFIGURATION
# =============================================================================
# After setting up your .env file:
# 1. Run: npm run dev
# 2. Go to: http://localhost:3000/dashboard/admin
# 3. Use "Email Testing" tab to verify email configuration
# 4. Use "Environment Test" tab to check all settings

# =============================================================================
# TROUBLESHOOTING
# =============================================================================
# If you encounter issues:
# 1. Check that all required variables are set
# 2. Verify email provider settings and ports
# 3. Ensure database file path is correct
# 4. Check that secrets meet minimum length requirements
# 5. Verify network connectivity for external services

# =============================================================================
# SUPPORT
# =============================================================================
# For configuration issues:
# - Check the admin dashboard environment testing
# - Review email testing results
# - Consult email provider documentation
# - Check application logs for detailed error messages

# =============================================================================
# HOSTINGER EMAIL SERVER INFORMATION (for reference)
# =============================================================================
# Incoming server (IMAP): imap.hostinger.com, Port: 993, SSL: Yes
# Incoming server (POP): pop.hostinger.com, Port: 995, SSL: Yes
# Outgoing server (SMTP): smtp.hostinger.com, Port: 465, SSL: Yes
# SMTP Authentication
SMTP_USER="noreply@herliva.com"
SMTP_PASS="Abd@3412"
# Email From Configuration
SMTP_FROM_NAME="English Learning Platform"
SMTP_FROM_EMAIL="noreply@herliva.com"